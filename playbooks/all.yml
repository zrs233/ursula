---

- hosts: all
  gather_facts: false
  connection: ssh
  sudo: yes
  tasks:
    - include: fireball.yml
    - action: fireball

- hosts: all
  connection: fireball
  tasks:
    - include: common.yml


- include: db.yml
- include: rabbit.yml
- include: keystone.yml
- include: glance.yml
