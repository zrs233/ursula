---

- name: update package index
  apt: update_cache=yes

- apt: pkg={{ item }}
  with_items:
    - python-pip
    - git
    - python-pip
    - python-libxml2
    - python-lxml
    - python-greenlet

- name: /opt/stack
  file: dest=/opt/stack state=directory
