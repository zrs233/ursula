---
- hosts: os-controller
  connection: fireball

  vars_files:
    - vars/common.yml

  tasks:

    - command: keystone-manage db_sync
    - command: glance-manage db_sync
    - command: nova-manage db sync
