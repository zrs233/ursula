---
- name: horizon venv
  # TODO: don't redo this if it's already done
  action: command python /opt/stack/horizon/tools/install_venv.py

- name: restart apache
  action: service name=apache2 action=restarted
