---
- name: Install and initialize corosync and pacemaker
  hosts: controller
  tasks:
  - include: openstack-ha/tasks/main.yml

# Onl configure pacemaker on the primary node
# and the settings will replicate to the secondary node.
- name: Init Pacemaker Conifguration
  hosts: controller[0]
  tasks:
  - include: openstack-ha/tasks/crm_init.yml
  - include: openstack-ha/tasks/crm_neutron.yml
