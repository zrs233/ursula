---
# TODO: remove this task list once nginx is gone everywhere

- shell: service nginx stop
  ignore_errors: True

- shell: apt-get autoremove -y nginx
  ignore_errors: True

- shell: rm -rf /etc/nginx
  ignore_errors: True

- shell: rm -f /etc/sensu/cond.d/checks/nginx.json
  ignore_errors: True
