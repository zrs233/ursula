# Send Swift messages async to a dedicated logfile
# action(
#   type="omfile"
#   file="/var/log/haproxy.log"
#   asyncWriting="on"
#   ioBufferSize="128KiB"
# )

if $programname startswith 'container' or $programname startswith 'object' or $programname startswith 'proxy' or $programname startswith 'account' or $programname startswith 'swift' then -/var/log/swift.log
& stop
