module(load="imfile") # needs to be done just once

{% for key, value in bbclog.follow.logs | dictsort %}

input(
  type="imfile"
  File="{{ value }}"
  Tag="{{ key }}"
  Ruleset="follow"
  Facility="local3"
  ReadMode="2"
  mode="inotify"
)
{% endfor %}
