# needs to be done just once
module(
  load="imfile"
  mode="inotify"
)

{% for key, value in bbclog.follow.logs | dictsort %}

input(
  type="imfile"
  File="{{ value }}"
  Tag="{{ key }}"
  Ruleset="follow"
  Facility="local3"
  ReadMode="2"
  StateFile="state-{{ key }}"
)
{% endfor %}
