# TODO(jdewey): Why can't this file be roles/x/default/main.yml
# http://www.ansibleworks.com/docs/playbooks_variables.html
#
# By using roles/x/vars/main.yml this overrides anything specified
# in the inventory (which is not what we want).

---
memcached:
  listen: 127.0.0.1
  port: 11211
  user: nobody
  memory: 64
  max_connections: 1024
