dn: olcDatabase={1}hdb,cn=config
changetype: modify
replace: olcSuffix
olcsuffix: {{ keystone.ldap.suffix }}
-
add: olcAccess
olcAccess: {0}to attrs=userPassword,shadowLastChange by self write by anonymou
 s auth by dn="i{{keystone.ldap.root_dn}}" write by * none
-
add: olcAccess
olcAccess: {2}to * by self write by dn="{{ keystone.ldap.root_dn }}" write by * read
-
replace: olcRootDN
olcRootDN: {{ keystone.ldap.root_dn }}
-
replace: olcRootPW
olcRootPW: {{ keystone.ldap.root_password }}

